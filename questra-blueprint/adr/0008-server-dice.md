# ADR-0008 — Server dice (CSPRNG) + physical-dice manual-entry mode
Accepted. Authoritative rolls are server-side with full derivations; client animation renders the server result. Per-campaign physical mode accepts typed raw die values flagged manual_entry. Rejected: client rolls (unauditable), forced-digital-only (kills in-person tables).
