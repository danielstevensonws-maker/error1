# ADR-0005 — The Engine never calls a model; every AI surface has a non-AI fallback
Accepted. Determinism stays deterministic; AI vendor downtime degrades drafting, never play. Rulings fall back to the difficulty ladder; portraits to the silhouette; prep to blank-page. Every AI feature ships its fallback in the same PR.
