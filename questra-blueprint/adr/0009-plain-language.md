# ADR-0009 — Plain-language UI vocabulary; ban list enforced in CI
Accepted. beat->scene, node->member/portrait; general rule: any word we must define is banned from the showroom. violatesPlainLanguage() in contracts; UI string tests must run it. The ban list grows by PR.
