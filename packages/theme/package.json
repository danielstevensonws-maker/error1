{
  "name": "@questra/theme",
  "version": "0.1.0",
  "private": true,
  "description": "The design token set (ADR-0014). Transcribed verbatim from Claude Design; every primitive reads --qa-* from here and hardcodes nothing.",
  "type": "module",
  "exports": {
    "./styles.css": "./src/styles.css",
    "./tokens.css": "./src/tokens.css"
  },
  "files": ["src"],
  "scripts": {
    "build": "echo \"@questra/theme is CSS-only — nothing to compile\"",
    "test": "vitest run",
    "check": "npm run build && vitest run"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
