{
  "name": "@questra/contracts",
  "version": "0.1.0",
  "description": "The shared spine: rules-data schemas, effect hooks, the play event vocabulary, formula language, fixtures. Features conform to this package; this package changes only by deliberate contract PR.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run build && tsc -p tsconfig.check.json && vitest run",
    "typecheck": "tsc -p tsconfig.check.json"
  },
  "dependencies": { "zod": "^4.4.3" },
  "devDependencies": { "typescript": "^7.0.2", "vitest": "^4.0.0", "@types/node": "^24.0.0" }
}
